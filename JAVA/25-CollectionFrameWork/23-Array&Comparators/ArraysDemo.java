import java.util.*;

class ArraysDemo {

    public static void main(String[] args) {
        int a[] = { 2, 4, 8, 3, 1, 9, 5 };
        int b[] = { 2, 4, 8, 3, 1, 9, 5 };

        // System.out.println(Arrays.compare(a, b));

        // int c[] = Arrays.copyOf(a, 5);
        // for (int x : c)
        // System.out.print(x + " ");

        // Arrays.fill(a, 1000);
        // for (int x : a)
        // System.out.print(x + " ");

        // Arrays.sort(a);
        // for (int x : a)
        // System.out.print(x + " ");
        // System.out.println("Index of 2 in a after Sorting " + Arrays.binarySearch(a,
        // 2));

    }
}